{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "jekyll serve",
            "command": "bundle exec jekyll serve",
            "type": "shell"
        },
        {
            "label": "jekyll build",
            "command": "bundle exec jekyll build",
            "type": "shell"
        }
    ]
}